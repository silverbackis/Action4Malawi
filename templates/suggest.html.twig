{% extends 'base.html.twig' %}

{% block stylesheets %}
  {{ parent() }}
  <link rel="stylesheet" href="{{ asset('build/css/form.css') }}" />
{% endblock %}

{% block body %}
  {% include '_hero.html.twig' with {
    header: 'Be part of the change!'
  } %}
  {% embed '_generalText.html.twig' %}
    {% block header %}
      Suggest a project for Action 4 Malawi
    {% endblock %}
    {% block text %}
      <p>We are just starting our work of connecting people. After the initial launch phase, we will be looking into further projects and developments.In the meantime, if you have a project you’d
        like to suggest, please register and write to us here. Please get in touch.</p>
      {{ form_start(form, {attr: {class: 'form-container', novalidate: 'true'}}) }}
      {{ form_widget(form) }}
      <div class="form-group mt-4 mb-0">
        <button class="btn btn-primary btn-lg-rounded" type="submit">Suggest Project</button>
      </div>
      {{ form_end(form) }}
    {% endblock %}
  {% endembed %}
{% endblock %}
